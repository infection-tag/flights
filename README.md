## File Structure:

```
your-project-folder/
├── index.html          (The main container and globe)
├── data.json           (Price info)       
└── destinations/       (Destination info)
    ├── kinshasa.js     
    └── (...)           (Other Cities)
```
